# BITÁCORA · INFINEXA

> Registro cronológico de sesiones de trabajo. Solo se agrega, nunca se borra ni se reescribe lo ya registrado — así siempre puedes reconstruir el orden exacto de lo que pasó. Las entradas más recientes van arriba.
>
> Para una visión del estado actual del proyecto (no histórico), ver `ESTADO.md`. Para prompts reutilizables, ver `RECETAS.md`. Este archivo es solo el "qué pasó y cuándo".

---

## 2026-07-14 (post #16 publicado — inflación en profundidad; limitación de sandbox en pipeline de imágenes documentada)

- **Post #16 escrito y publicado:** "El día que el dinero dejó de alcanzar: lo que pasa cuando la inflación deja de ser silenciosa" (`_posts/2026-07-14-inflacion-en-profundidad.md`, pilar Diversifica). Profundiza el post #8 ("El impuesto silencioso") sin repetirlo — verificado contra unicidad de H2 y ganchos de los 15 posts existentes antes de escribir. Cubre: origen monetario de la inflación (Friedman, demanda vs. oferta), cómo se mide de verdad (canasta INPC/subyacente, con dato real verificado de cierre 2025 en México: 3.69% general / 4.33% subyacente, fuente INEGI), y 4 casos históricos de hiperinflación (Alemania 1923, Hungría 1946, Argentina 1989, Venezuela 2018) usando el componente `.timeline-row` ya existente en el CSS del layout. 3 bloques `.principio` en orden 1→2→3 con texto de transición entre cada uno (verificado con grep antes de publicar). Título de imagen ("Cuando el dinero pierde el control") distinto al `title` del front matter, siguiendo la regla 6. Callback a Carlos en el cierre (no en la apertura, a diferencia del post #8) para variar la posición, siguiendo la regla de unicidad de `PROMPT_BLOG.md`.
- **Investigación con fuentes reales antes de escribir:** inflación de México cierre 2025 (INEGI, La Jornada, México Cómo Vamos), casos históricos de hiperinflación (Portafolio, El Nacional, Wikipedia), metodología de medición de inflación/CPI (Cleveland Fed, BLS, Brookings).
- **Limitación de infraestructura descubierta y documentada — pipeline de imágenes Playwright no disponible en este entorno:** al intentar generar la imagen destacada con el proceso estándar (`generar_destacadas.py`, Playwright + Chromium, documentado en `ESTADO.md` sección 3.7), Chromium no pudo instalarse por completo en este sandbox — faltan librerías de sistema (`libxdamage1` y otras) y no hay acceso `sudo`/root para instalarlas (`apt-get` falla por permisos). **Solución aplicada para no bloquear la sesión:** se generó la imagen con `cairosvg` (extrayendo el mismo SVG que produce `build_svg()` en el script real, con el mismo path SVG exacto del logo — no redibujado a mano) en vez de Chromium, y con la fuente `Lato` como sustituto de `Inter` (no disponible en el sandbox; se intentó descargar el archivo de fuente real pero la red del sandbox no tiene ese dominio permitido). Resultado verificado contra el checklist de 4 puntos, incluida una simulación real a 390px de ancho — aprobado por Alejandro. **Pendiente real, no oculto:** si se quiere el render exacto con Chromium/Inter (fidelidad 100% al pipeline documentado), regenerar `inflacion-en-profundidad` desde una sesión con Playwright funcional (Claude Code local, o una sesión de Claude.ai con el entorno correcto) — no es necesario para las imágenes ya publicadas anteriormente, que sí se generaron con el pipeline real.
- **Incidente menor de sandbox — `.git/index.lock` y `__pycache__/` no se podían borrar con `rm`:** el mismo tipo de restricción de borrado de la carpeta de trabajo conectada (que normalmente aplica a entregables) también bloqueó archivos técnicos temporales creados durante la sesión (`.git/index.lock` stale, `__pycache__/`). Resuelto pidiendo permiso explícito de borrado antes de continuar con `git add`/`commit`. Se agregó `__pycache__/` a `.gitignore` para que no vuelva a aparecer como archivo sin trackear en sesiones futuras que ejecuten el script de imágenes con Python directamente (en vez de solo Playwright).
- **`sitemap.xml` actualizado** con la entrada del post #16 (`lastmod: 2026-07-14`).
- **Verificación final en GitHub:** clonado del repo a `/tmp/check-post` confirmando que el `.md`, el `.webp` y la entrada de `sitemap.xml` llegaron correctamente. Commit `6b9f284`.
- **Archivos actualizados:** `_posts/2026-07-14-inflacion-en-profundidad.md` (nuevo), `assets/blog/inflacion-en-profundidad.webp` (nuevo), `_plantillas/sistema-imagenes-blog/generar_destacadas.py` (+1 entrada en CONFIGS), `sitemap.xml`, `.gitignore`, `ESTADO.md` (sección 4.4 y sección 8), `BITACORA.md` (esta entrada).

## 2026-07-13 (sesión — retrofit final a includes compartidos)

- Migradas las últimas 4 páginas con código duplicado en línea (`/`, `/servicios/`, `/infografia/`, `/diversifica/`) a los includes compartidos `_includes/ga4.html`, `_includes/analytics.html`, `_includes/qr-share.html` y `_includes/reduced-motion.html` — el mismo conjunto que ya usaban las 5 páginas nuevas de Fase 1.5 y el layout del blog. Commit `0650ff8`.
- Cuidado especial: en home, servicios, infografia y diversifica, la función `trackEvent()` vivía mezclada dentro de un `<script>` más grande con lógica propia de cada página (auto-wiring de CTAs, FAQ, lead-form, disparo automático de eventos). Se extrajo solo la definición de `trackEvent()` hacia el include, partiendo el `<script>` original en dos donde fue necesario, sin tocar el resto de la lógica.
- Se detectó y corrigió, antes de hacer commit, una duplicación propia introducida al migrar el QR de `/infografia/`: quedó un bloque de inicialización de QR duplicado (el que ya trae `qr-share.html` más un remanente del original) — corregido eliminando el remanente.
- Verificado en vivo (Claude in Chrome) en las 4 páginas tras el deploy: `window.gtag` y `window.trackEvent` definidos correctamente, el QR se renderiza una sola vez (2 hijos esperados: canvas + texto de URL, no duplicado), el logo de infografia sigue apareciendo una sola vez, y la clave de Web3Forms de servicios se mantiene intacta (`window.INFINEXA_WEB3FORMS_KEY`).
- Se conservó intencionalmente el footer con firma embebida en las 4 páginas (no se migró a `footer.html`, para no duplicar la firma — mismo criterio de Home en fases anteriores).
- Con esto, todos los pendientes reales listados al inicio de esta sesión quedan resueltos: Web3Forms, responsive (salvo el extremo grande de pantalla), verificación de eventos GA4, y retrofit de includes.

## 2026-07-13 (sesión — verificación real de eventos GA4 en producción)

- Alejandro abrió el reporte "Resumen en tiempo real" de Google Analytics (infinexa.app) mientras Claude navegaba el sitio en vivo con la extensión de Claude in Chrome.
- Confirmado en pantalla, con nombre exacto de evento y conteo: `contact_form_submitted` (1, del envío de prueba de Web3Forms hecho minutos antes), `diversification_assessment_started` (1, clic en el botón "Evaluar mi nivel de diversificación" de `/diversifica/`), `innovation_guide_opened` (1, disparo automático al cargar `/infografia/`), `transparency_page_opened` y `risk_page_opened` (1 cada uno, clics en los links del footer "Transparencia"/"Riesgos" desde Inicio).
- Esto confirma 5 de los 21 eventos personalizados llegando correctamente a GA4 en producción, cubriendo los tres mecanismos de disparo que usa el sitio: envío de formulario, clic en CTA, y carga automática de página. No se verificó cada uno de los 21 individualmente, pero el patrón queda validado de punta a punta — ya no es solo una confirmación de que el código dispara `trackEvent()`, sino de que Google Analytics efectivamente lo recibe y clasifica.
- Con esto, los 4 pendientes marcados como prioritarios en la sección 8 de `ESTADO.md` (Web3Forms, responsive, retrofit de includes aún abierto, y verificación de GA4) quedan resueltos salvo el retrofit de includes (que no era bloqueante) y la verificación visual del extremo grande de pantalla (1440–2048px).

## 2026-07-13 (sesión — clave real de Web3Forms conectada y verificada)

- Alejandro compartió la access key real de Web3Forms (creada en web3forms.com). Se reemplazó el placeholder `WEB3FORMS_ACCESS_KEY_PENDIENTE` en los dos únicos lugares donde vivía: `_includes/form-handler.html` (usado por `/diagnostico/`, `/talleres/`, `/contacto/`) y la copia inline de `servicios/index.html` (nunca migrada al include compartido). Commit `dda4a53`.
- Con permiso explícito de Alejandro, se hizo un envío de prueba real usando la extensión de Claude in Chrome (ya conectada): se llenó y envió el formulario de `/contacto/` con datos claramente marcados como prueba ("PRUEBA - Claude (verificación Web3Forms)"), consentimiento marcado. El formulario respondió "Listo — recibimos tu información. Te contactaremos pronto." (respuesta de éxito de la API de Web3Forms) y se reseteó correctamente, mostrando además el seguimiento de WhatsApp opcional.
- Con esto, los 4 formularios del sitio (diagnóstico, talleres, contacto, servicios) quedan confirmados como funcionales de punta a punta — no solo validando la UI, sino la entrega real vía la API de Web3Forms.
- Pendiente: que Alejandro confirme haber recibido el correo de prueba en su bandeja.

## 2026-07-13 (sesión de pruebas responsive — extensión de Claude in Chrome conectada por primera vez)

- Alejandro instaló y conectó la extensión de Claude in Chrome (`list_connected_browsers` confirmó el dispositivo). Primer intento real de pruebas responsive visuales de la sesión.
- Se intentó `resize_window` a 375/768/1024/1440/2048px sobre una pestaña en `infinexa.app/` — el comando reportó éxito pero `window.innerWidth` se quedó fijo en 1280px en los 5 casos (confirmado con `javascript_tool` leyendo `window.innerWidth` tras cada resize). Se intentó también simular anchos vía zoom del navegador (`ctrl+=`, `cmd+=`) — tampoco cambió el viewport real. Ambos métodos parecen no tener efecto en el entorno de esta sesión (posible limitación de la ventana/pantalla virtual del sandbox).
- Ante esta limitación, se hizo en su lugar una verificación real (no solo por código fuente) al único ancho disponible de forma fiable (1280px, dentro del rango 900–1400px de los breakpoints): se navegó y midió `getBoundingClientRect()`/`getComputedStyle()` del `.card` en `/`, `/aprender/`, `/contacto/`, `/blog/`, `/infografia/`, `/diversifica/` y `/servicios/` — las 7 superficies devolvieron exactamente 1200px de ancho de shell, confirmando en un navegador real (no por inspección de código) que la unificación de Fase 1.5B/1.5D sigue vigente.
- Capturas de pantalla tomadas confirman: el botón "Hablemos" de `/contacto/` no se parte en dos líneas (una sola línea, el bug original de Fase 1.5A sigue corregido); `/infografia/` muestra el logo de Infinexa una sola vez (el fix de Fase 1.5E sigue vigente, verificado visualmente por primera vez, no solo por fetch de HTML).
- **Pendiente real, no oculto (al momento):** no se logró verificar visualmente los anchos por debajo de 900px ni por encima de 1400px desde esta sesión — el entorno no permitió forzar esos viewports vía `resize_window`/zoom. Resuelto parcialmente minutos después (ver entrada siguiente): Alejandro verificó el extremo móvil manualmente con Chrome DevTools.

**Actualización — verificación manual de Alejandro con Chrome DevTools (mismo día):** usando el device toolbar de DevTools (Cmd+Shift+M) con el preset iPhone SE (375×667), Alejandro compartió 3 capturas: (1) clic en "Aprender" desde `/` — confirma navegación correcta a `/aprender/` con el link marcado activo; (2) `/contacto/` a 375px — nav se acomoda en dos líneas (comportamiento esperado, no bug), formulario y contenido completos sin cortes; (3) `/` a 375px — hero y CTA completos sin desbordes. Con esto, el rango móvil (375px, vía DevTools real) y el rango medio (1280px, vía medición en esta sesión) quedan verificados en un navegador real. **Sigue pendiente únicamente** el extremo grande (1440–2048px, el ancho exacto del bug original de Fase 1.5B) — respaldado solo por consistencia de código fuente hasta ahora.

## 2026-07-13 (sesión Fase 1.5E — eliminación de logo duplicado + regla global de prevención)

- Detectado, después de integrar `/infografia/` y `/diversifica/` en Fase 1.5D, que ambas seguían mostrando el logo de Infinexa dos veces: una vez pequeño en `{% include nav.html %}` (agregado en 1.5D), otra vez grande en el bloque `.ifx-lockup` heredado de su propio `.hdr` (nunca eliminado en la migración). Bug de la misma familia que el ya corregido en `/servicios/` durante Fase 1.5A (issue #8 de aquella auditoría).
- Corregido en ambas páginas: eliminado el bloque HTML `.ifx-lockup` de `.hdr` (se conserva el resto del header intacto) y el CSS huérfano resultante (`.ifx-lockup`, `.ifx-sep-v`, `.ifx-wm`, `.ifx-name`, `.ifx-line`, `.ifx-tag`), verificado que ninguna otra clase los reutilizaba. Commit `5bb4619`.
- Agregada una regla global de prevención en `_gestion/RECETAS.md` §1.2: checklist de 4 pasos obligatorio para cualquier futura migración de una página a `nav.html` compartido, para que este bug no se repita una tercera vez (aplica a `donativo/`, `transparencia/`, `riesgos/`, `privacidad/`, `terminos/` si se integran más adelante).
- Verificado balance de `<div>`/`<svg>`/Liquid en ambos archivos y contenido en vivo post-deploy: el logo aparece una sola vez, resto del contenido/CTAs/disclaimers sin cambios.
- Dictamen: **GO**.

## 2026-07-13 (sesión Fase 1.5D — integración de /infografia/ y /diversifica/)

- Última de las páginas "preexistentes" (junto con `/servicios/`, ya normalizada en 1.5A) que seguían fuera del sistema unificado de shell/nav/tokens. Auditoría previa confirmó: sin front matter de Jekyll, nav hardcodeada con el mismo bug de "Aprender" → `/blog/` ya corregido en 1.5B para otras páginas, y un tercer valor de ancho de shell (1100/1200px) distinto al resto del sitio (1200/1320px).
- Ejecutado sobre ambas páginas: front matter vacío + `{% include design-tokens.html %}`; ancho del shell migrado a los tokens compartidos; nav reemplazada por `{% include nav.html wa_text="..." %}`; `.nav-links` gap normalizado; tipografía del hero (`.hero-h1`) migrada a `var(--text-hero)`; anchos de lectura migrados a `var(--reading-max-width)`; agregado JSON-LD `BreadcrumbList` (único elemento SEO nuevo). Commit `1f8537c`.
- Decisión de alcance explícita: se dejó GA4/analytics/QR/reduced-motion/footer inline (sin migrar a includes) y el botón CTA verde de WhatsApp sin tocar, siguiendo el precedente de `/servicios/` (1.5A) y la instrucción explícita del prompt de conservar el carácter de "recurso educativo especial" de estas páginas.
- Revisión editorial: sin lenguaje absolutista o promocional encontrado; disclaimer específico de Bitcoin en `/infografia/` conservado intacto.
- Verificado balance de `<div>`/Liquid/`<script>`, JSON-LD válido, y contenido en vivo post-deploy (nav con "Aprender" → `/aprender/`, todo el contenido/CTAs intactos).
- Dictamen: **GO**.

## 2026-07-13 (sesión Fase 1.5B — corrección de navegación y unificación del shell exterior)

- Ejecutados, en la misma sesión continua, tres prompts sucesivos sobre Fase 1.5: (1) integración estratégica del perfil de MBA Alejandro García como narrativa de liderazgo en todo el sitio — dictamen GO; (2) Fase 1.5A, normalización visual/estructural de 7 páginas con tokens de diseño compartidos (`_includes/design-tokens.html`) — dictamen GO; (3) Fase 1.5B, corrección de dos bugs confirmados tras 1.5A. Ver `ESTADO.md` §1.12–1.14 para el detalle completo de los tres.
- **Fase 1.5B — Problema A (nav "Aprender"):** confirmado que `index.html` conservaba un bloque de nav hardcodeado con el enlace obsoleto `/blog/` para "Aprender" (nunca migrado al include compartido en 1.5A) — corregido con `{% include nav.html active="inicio" %}` (commit `c881955`). En la verificación en vivo posterior se encontró el mismo bug, no reportado, en `_layouts/default.html` (layout del blog, con su propio nav `.blog-nav` independiente) — corregido de la misma forma (commit `fed7562`).
- **Fase 1.5B — Problema B (ancho del shell):** confirmado que Fase 1.5A normalizó las 6 páginas interiores a un ancho (1040/1100px) distinto del real de Home (1200/1320px) y del real del layout del blog (1100/1200px, un tercer valor). Corregido redefiniendo `--content-max-width` en `_includes/design-tokens.html` a 1200px (el valor real de Home), actualizando el breakpoint de 1400px de las 6 páginas interiores + `servicios/` a `var(--site-max-width)`, migrando `index.html` a los tokens compartidos, y agregando el include de tokens + los mismos valores a `_layouts/default.html`. Las 8 superficies del sitio (`/`, `/aprender/`, `/diagnostico/`, `/talleres/`, `/servicios/`, `/nosotros/`, `/contacto/`, `/blog/`) comparten ahora el mismo `max-width` de shell.
- Verificado balance de `<div>`/Liquid en los 9 archivos tocados antes de cada commit. Verificado en vivo post-deploy (con cache-busting) que Home y el blog (listado + post individual) apuntan "Aprender" a `/aprender/`.
- **Pendiente real, no oculto:** pruebas responsive visuales reales en navegador (Claude in Chrome no conectado en esta sesión — mismo pendiente arrastrado desde Fase 1.5/1.5A); el JSON-LD `BreadcrumbList` de `_layouts/default.html` sigue nombrando "Aprender" con `item: https://infinexa.app/blog/` — no corregido por estar fuera del alcance visual/estructural de esta fase.
- Dictamen Fase 1.5B: **GO**.

## 2026-07-13 (sesión Fase 1.5 — consolidación, medición y captación)

- Guardados como archivo, por primera vez, los dos prompts maestros que hasta ahora solo existían en el historial de conversación: `_gestion/PROMPT_MAESTRO_REPOSICIONAMIENTO_13JUL2026.md` (el prompt de 31 secciones que gobernó las Fases 1-8, commit `cbb2b86`) y `_gestion/PROMPT_FASE_1.5_CONSOLIDACION_13JUL2026.md` (el nuevo prompt de 22 secciones, commit `2f3e847`).
- Auditado el cumplimiento de los 31 puntos del prompt maestro original contra el repo real (no contra la memoria de la conversación) antes de recibir el segundo prompt — dictamen entregado: GO WITH CONDITIONS.
- Ejecutado el prompt de Fase 1.5 de punta a punta, sin pausar entre bloques salvo por un bloqueante real (destino técnico de los formularios — resuelto con la elección explícita de Alejandro por Web3Forms).
- Creados 7 includes compartidos de Jekyll (`_includes/nav.html`, `footer.html`, `ga4.html`, `analytics.html`, `qr-share.html`, `reduced-motion.html`, `form-handler.html`) y 5 páginas dedicadas nuevas (`/diagnostico/`, `/talleres/`, `/nosotros/`, `/contacto/`, `/aprender/`) — commit `ff1b4fa`. Verificado en vivo tras el push: sin errores de Liquid, balance de HTML correcto.
- Actualizada la navegación de las 10 superficies preexistentes del sitio (incluyendo 2 CTA del hero del home) para apuntar a las páginas dedicadas nuevas en vez de anclas del home — commit `0604eb2`. Se detectaron y corrigieron a mano 2 enlaces sueltos que el `sed` masivo no capturó (uno en `diversifica/index.html`, otro en `transparencia/index.html`).
- Agregados canonicals a las 9 páginas preexistentes, normalizando a la forma con slash final (se detectó y corrigió que `servicios` e `infografia` tenían `og:url` histórico sin slash — no se copió tal cual). JSON-LD `WebSite`/`Organization`/`Person` en el home y `BlogPosting`/`BreadcrumbList` en los 15 posts del blog vía `_layouts/default.html`. Navegación "artículo siguiente/anterior" sistemática vía `page.next`/`page.previous` de Jekyll en `_layouts/post.html`, reemplazando cualquier enfoque manual futuro. Accesibilidad (`prefers-reduced-motion`, skip-link, `:focus-visible`) retrofit a las 9 páginas preexistentes sin alterar su estructura de `<div>` — commits `547d189`/`f0e62ed`.
- Agregado formulario de captación de leads a `/servicios/` (Infinexa Digital) — commit `6500550`.
- Completados los 21 eventos de GA4 (13 nuevos sobre los 8 que ya existían), incluyendo agregar la opción "Conferencia para mi empresa/evento" al formulario de talleres para poder distinguir `conference_interest_submitted` de un registro de taller normal, y wiring de `transparency_page_opened`/`risk_page_opened` en el footer de las 14 superficies del sitio (antes solo disparaban desde 5) — commit `0b90e4a`.
- Documentada internamente (sin ejecutar código) la condición para el futuro de la situación de Carlos, por instrucción explícita del prompt: cualquier landing dedicada futura debe vivir fuera de la identidad de Infinexa y requiere comunicación previa con él — agregado como adenda a `ESTADO.md` §1.10.
- **Intentado 3 veces** conectar la extensión de Claude in Chrome para pruebas responsive reales en 320/375/390/768/1024/1440px — no disponible en esta sesión. No se afirmó ninguna prueba visual que no se haya realizado; queda como pendiente real documentado en `ESTADO.md` §1.11 y §8.
- **Pendiente real, no oculto:** clave de acceso real de Web3Forms (bloqueante para que los 4 formularios entreguen correo), retrofit completo de las 9 páginas preexistentes a los includes compartidos (dedupe de HTML — se decidió no forzarlo por riesgo de refactorización masiva), pruebas responsive visuales reales, y confirmación desde la cuenta real de GA4 de que los 21 eventos están llegando correctamente.
- `ESTADO.md` actualizado con la nueva sección 1.11 (detalle completo de Fase 1.5) y sección 8 (próximos pasos) revisada.

## 2026-06-19 (sesión separada, Claude.ai — plan de marketing orgánico)

- Verificado, mediante el HTML real de `index.html` e `infografia/index.html`, que los tres ajustes de contraste de color (borde blanco transparente en íconos de acordeón `rgba(255,255,255,0.65)`, fondo petróleo denso en badges `rgba(27,77,92,0.35)`, cobre brillante en cursivas `#D97B3C`) ya estaban aplicados en producción — no se trataba de un pendiente real, solo de una duda a resolver. Confirmado: sin acción necesaria.
- Creada carpeta `_marketing/` en el repo, separada de `_gestion/`, para contenido de marketing de atracción (planes de contenido, copys de publicación) — distinto del estado técnico (`ESTADO.md`) y los prompts reutilizables (`RECETAS.md`).
- Generado `_marketing/PLAN-ORGANICO.md`: plan de 4 semanas / 12 publicaciones para Facebook, dirigido a la red personal de Alejandro (2,000+ contactos, audiencia tibia, sin contenido previo publicado sobre economía digital o Hand4Hand).
- Estructura del plan en escalera de convicción de 4 fases: Despertar (semana 1, sin mencionar H4H) → Educación (semana 2, sigue sin vender) → Revelación (semana 3, primera mención de H4H + primer CTA suave) → Conversión (semana 4, experiencia propia + CTA directo).
- Copiadas a `_marketing/assets/` las 4 imágenes generadas para WhatsApp en la conversación de Claude.ai: `arte_infinexa_wa.png`, `arte_infinexa_estado.jpg`, `infinexa_whatsapp.png`, `infinexa_estado.jpg`.
- Agregada receta nueva (sección 10) a `RECETAS.md`: patrón reutilizable de "plan de contenido orgánico por escalera de convicción", aplicable a cualquier red social a futuro, no solo Facebook.
- Agregado enlace desde `ESTADO.md` §7 (Estrategia de prospección) hacia `_marketing/PLAN-ORGANICO.md`, sin modificar el resto de esa sección.
- Subido a GitHub en el commit `f1046dc`: `_marketing/PLAN-ORGANICO.md`, las 4 imágenes en `_marketing/assets/`, y el addendum de `RECETAS.md`.
- **Nota de proceso:** esta sesión se trabajó en una conversación distinta (Claude.ai, no Claude Code), por lo que se siguió un protocolo de verificación antes de escribir: se pidió el contenido real de `ESTADO.md`, `RECETAS.md`, `BITACORA.md` y el HTML en vivo de ambas páginas antes de asumir qué información era nueva, cuál ya existía, y cuál podía contradecir decisiones más recientes tomadas en otras sesiones (Claude Code).

## 2026-06-19

- Diagnosticado y corregido bug del `apple-touch-icon` de Diversifica: el archivo tenía canal alfa (RGBA/transparencia), lo que causaba distorsión visual al anclar la página a la pantalla de inicio en iOS (aunque la imagen de Open Graph, que sí era RGB sin transparencia, se veía perfecta).
- Regenerados desde cero los tres archivos de íconos de marca (`favicon.png` 32×32, `apple-touch-icon.png` 180×180, `og-image.png` 1200×630) directamente desde el SVG vectorial fuente (`infinexa-icono.svg` / `infinexa-logo-negativo.svg`), confirmando en cada uno modo RGB sin transparencia antes de subirlos.
- **Descubierta la causa raíz real:** existían dos carpetas paralelas de assets — `assets/` (la que de verdad usan la carta, infografía, servicios y el builder `carlos`) y `infinexa-assets/files/` (carpeta huérfana, sin ninguna referencia real, creada por error en una sesión anterior). Diversifica apuntaba a la carpeta huérfana, que tenía una versión vieja con transparencia — por eso se veía mal mientras las demás páginas (que ya usaban `assets/`, correcto desde antes) se veían bien.
- Verificado con `grep -rn "infinexa-assets" --include="*.html" .` que ninguna página dependía de la carpeta huérfana antes de eliminarla.
- Corregidas las rutas de `diversifica/index.html` para usar `/assets/` (consistente con el resto del sitio) en lugar de `infinexa-assets/files/`.
- Eliminada por completo la carpeta `infinexa-assets/` (8 archivos, incluyendo `.DS_Store` y el script `aplicar-og.sh` que ya no se usaba). Commit `872c0c0`.
- **Lección para futuras sesiones:** antes de crear una carpeta nueva de assets, verificar primero con `grep -rn "assets" --include="*.html" .` si ya existe una convención establecida en el repo — evita duplicar carpetas con el mismo propósito.
- Confirmado con captura de WhatsApp que la vista previa de Diversifica ya se ve idéntica en calidad a la de la carta principal, y confirmado por el usuario que el ancla a pantalla de inicio en iPhone ya se ve nítida.
- Iniciada esta bitácora y `RECETAS.md` como sistema de organización para futuras sesiones.
- **(Sesión separada, mismo día) Auditoría y optimización del sistema de documentos de marca:** se comparó el `Manual de Marca` (PDF v1.0) contra el `Brand Identity Brief` (DOCX) para determinar si debían fusionarse. Se concluyó que no — cubren audiencias distintas (estrategia/voz vs. especificación técnica/visual) — pero sí debían optimizarse como sistema conectado.
- Reconstruidos ambos documentos en formato DOCX, v1.1, cada uno con una sección inicial "cómo usar este sistema de dos documentos", eliminando la duplicación de la paleta de color y la tipografía (que ahora vive completa solo en el Brief, con referencia desde el Manual), agregando cajas de referencia cruzada entre ambos, y numerándolos como "Documento 1 de 2" / "Documento 2 de 2".
- Investigadas con búsqueda web las políticas vigentes (2026) de Meta Ads, Google Ads y WhatsApp Business para contenido financiero/cripto, además del contexto regulatorio de CONDUSEF/CNBV en México sobre esquemas piramidales disfrazados de "red de marketing" o "academia de inversión".
- Agregada al `Manual de Marca` una sección nueva — "07 — Cumplimiento y políticas de plataformas" — con lineamientos prácticos para evitar baneos/spam en cada plataforma, y dos ítems nuevos en el checklist de coherencia (anuncios pagados y mensajería de WhatsApp). Sección renumerada consecutivamente (Coherencia pasó de sección 06 a 08).
- **Corregida la paleta de color de ambos documentos:** faltaban 2 colores que ya estaban documentados en `ESTADO.md` §3.4 (Grafito oscuro `#0F1720`, fondo principal negativo; Blanco marca `#F8F8F6`, fondo principal positivo) — se detectó la discrepancia comparando contra el `ESTADO.md` real antes de tocar los documentos, y se agregaron a ambos.
- Agregada nueva receta a `RECETAS.md` (sección 9): cómo auditar/optimizar un sistema de documentos de marca en dos capas, y cómo agregar secciones de cumplimiento legal/políticas investigando primero la información vigente.
- **Pendiente para la próxima sesión:** subir `Infinexa_Manual_de_Marca.docx` e `Infinexa_Brand_Identity_Brief.docx` al repositorio, y eliminar las versiones anteriores (PDF del Manual v1.0 y el `infinexa-brand-brief.docx` viejo) una vez confirmado que el contenido fue migrado completo.

## 2026-06-17 (sesión larga — Diversifica completa)

- Construida página Diversifica desde cero: 10 bloques narrativos sobre diversificación de ingresos, con enfoque progresivo hacia la economía descentralizada.
- Primera versión en formato carrusel de pantalla completa — **abandonada por completo** tras detectar que bloqueaba el zoom nativo del navegador en móvil (el gesto de pinch-zoom avanzaba al siguiente slide en vez de ampliar texto).
- Reconstruida completa en formato scroll vertical normal, replicando el sistema visual ya validado de la infografía (header con lockup, badge superior, acordeones, tipografía en clamps).
- Resuelto bug de CSS: un media query de `max-width:720px` estaba posicionado *antes* de las reglas base de tipografía — en la cascada CSS, eso hacía que las reglas base (más abajo) sobreescribieran silenciosamente las correcciones de móvil. **Lección permanente: los media queries siempre van al final del bloque `<style>`.**
- Agregado el set completo de Open Graph y Twitter Card (la primera versión no tenía ninguno, por lo que no se generaba vista previa al compartir).
- Auditoría completa de copy para claridad: 9 frases identificadas como confusas o ambiguas, todas reescritas (incluyendo la frase del trueque/efectivo que generó la auditoría completa).
- Investigado y verificado el dato de "fuentes de ingreso de millonarios": 65% de millonarios hechos a sí mismos tienen al menos 3 fuentes de ingreso (no el rumor de "5 a 7 fuentes" que no tenía buen respaldo). Usado en la página y en la infografía de WhatsApp, con el término "libertad financiera" en vez de "millonarios" para sonar más alcanzable.
- Reforzado en tres puntos de la página el enfoque específico hacia la economía descentralizada como el terreno concreto donde se construye la "siguiente fuente de ingreso" — incluyendo la idea de velocidad/ventana de oportunidad temprana.
- Página publicada en `infinexa.app/diversifica`.
- Agregados `sitemap.xml` y `robots.txt` en la raíz del repo; verificado dominio en Google Search Console (método archivo HTML); indexadas la carta, la infografía y Diversifica (servicios quedó pendiente por decisión propia).
- Confirmado en Meta Debugger que la vista previa de Diversifica funciona correctamente (advertencia menor no bloqueante de `fb:app_id`, decidido no resolver).
- Generada infografía para WhatsApp ("¿Cuántas fuentes de ingreso tienes tú?") con el dato del 65%, exportada a PNG vía Playwright (62.8 KB).
- `ESTADO.md` actualizado dos veces durante la sesión (sección 4.1 Diversifica, sección 4.2 SEO e indexación), verificando contra la versión real del repo antes de cada actualización para no perder ediciones manuales.

---

## 2026-07-13 (decisión de reposicionamiento total — eliminación de Hand4Hand de la carta, Fase 1)

- **Decisión estratégica de Alejandro:** Infinexa deja de ser un funnel de prospección hacia Hand4Hand y pasa a ser una plataforma independiente de educación financiera/tecnológica (dinero, blockchain, Web3, IA, nuevos modelos de creación de valor), con una futura ruta de "aplicación práctica" que puede o no involucrar terceros, sin nombrarlos en la home.
- **Auditoría completa del repo** antes de tocar nada: confirmado que Hand4Hand solo vivía en `index.html` (Escalón 4/5 y CTA final) y en el sistema de builders (`builders/carlos/`, `builders/_template/`, `carlos/`) — Servicios, Diversifica, Infografía y los 15 posts del blog ya estaban limpios.
- **Fase 1 ejecutada y publicada (commit `06747d6`):** eliminado el Escalón 4 (mecánica Ciclo 2×2 — el mismo hallazgo de riesgo de cumplimiento del 23 jun, nunca ejecutado hasta hoy) y el Escalón 5 (SI/NO, describía el producto de Hand4Hand). Eliminado el roadmap de onboarding H4H del CTA (Génesis, Human Wallet, Sprint 72h, Insider→Maker). Reescritos Bridge, CTA, mensaje de WhatsApp y disclaimer final para hablar de Infinexa en general, sin nombrar Hand4Hand. Limpiado CSS huérfano de los bloques eliminados.
- **Hallazgo de seguridad no relacionado, corregido de paso:** `ESTADO.md` no tenía front matter ni estaba en el `exclude:` de `_config.yml` — Jekyll lo copiaba tal cual al build como archivo estático descargable en `infinexa.app/ESTADO.md`, exponiendo públicamente los hallazgos internos de cumplimiento (incluida la nota de que la mecánica de ciclos es "estructuralmente igual a un esquema Ponzi") y la estrategia de negocio. Agregado a `exclude:`.
- **`_config.yml` — descripción del sitio corregida:** de "Educación y prospección en la nueva economía digital" a una descripción sin lenguaje de reclutamiento.
- **No tocado, requiere decisión explícita de Alejandro:** `builders/carlos/`, `builders/_template/` y `carlos/` en la raíz — son landings completas de Hand4Hand, y `carlos.infinexa.app` está en vivo para un tercero real. Apagar o reescribir esas páginas sin avisar tiene implicaciones de relación/negocio que no me correspondía decidir solo. Ver `ESTADO.md` sección 1.1 y "Próximos pasos" ítem 1b.
- **Flujo de git usado:** se encontró un `.git/index.lock` residual que el sistema de archivos del sandbox no dejaba borrar (protección de borrado de la carpeta conectada) — resuelto solicitando permiso de borrado explícito antes de continuar.
- **Alcance total pactado con Alejandro:** el pedido completo (nueva arquitectura de marca en 3 pilares, home de 13 secciones, nav unificado — el sitio no tiene header/nav compartido, cada página es HTML standalone —, diagnóstico, páginas nuevas de transparencia/riesgos/privacidad/términos, auditoría de claims sensibles del blog, analítica, SEO, accesibilidad) es una reconstrucción de varias sesiones. Se acordó ejecutar por fases con commit y push al cierre de cada fase (no todo junto, no solo local). Fase 1 (esta) cerrada; fases siguientes pendientes de sesión futura.

## 2026-07-13 (continuación) — Fase 2: nueva home de 13 secciones y navegación unificada

- **`index.html` reconstruido por completo** como la nueva home de Infinexa, siguiendo el prompt maestro: hero, "el cambio ya comenzó", "el verdadero problema", qué puedes aprender (10 tarjetas con enlaces verificados a posts reales del blog y a Diversifica/Infografía), tres rutas (Comprender/Evaluar/Aplicar), Método Infinexa (5 pasos), Diagnóstico (quiz funcional de 3 preguntas, 100% client-side, sin backend — genera un mensaje de WhatsApp con el resultado), "del conocimiento a la práctica", Talleres (formulario funcional que arma un mensaje de WhatsApp con nombre + tema), Infinexa Digital (teaser hacia `/servicios/`), Quién está detrás (perfil de Alejandro, solo datos confirmados, sin inventar certificaciones), Principios, CTA final.
- **Nav unificada** (Inicio · Aprender · Diagnóstico · Talleres · Servicios · Nosotros + botón "Hablemos") agregada a las 6 superficies del sitio: home, servicios, diversifica, infografía, donativo y el layout del blog (`_layouts/default.html`, antes tenía Inicio/Diversifica/El Patrón/Blog). Diagnóstico/Talleres/Nosotros son anclas dentro de la home (no páginas propias todavía) — funcionan desde cualquier página vía `/#ancla`.
- **Enlaces verificados antes de publicarlos:** los slugs de las 10 tarjetas de "qué puedes aprender" se confirmaron contra el front matter real de `_posts/` (no se asumieron desde el título) — mismo protocolo que ya exige `PROMPT_MAESTRO.md` Regla de Oro #5, para evitar el error de 404 documentado el 6 jul.
- **Analítica:** se agregó un helper `trackEvent()` instrumentado en los CTA, pero confirmado que no hace nada útil todavía — no hay GA4/GTM instalado en el sitio (se verificó con grep, cero rastro previo). Queda como pendiente explícito: Alejandro debe dar un ID de medición para activarlo.
- **Verificado en vivo tras el push** (`51caf8a`): home, blog y servicios muestran la nav nueva correctamente; contenido de Diversifica/Infografía/Servicios permanece sin cambios (solo se les agregó la nav, su reposicionamiento de contenido queda para una fase futura).
- **`ESTADO.md` actualizado** con la sección 1.2 documentando esta fase y el detalle de qué falta.

## 2026-07-13 (continuación) — Fase 3: reposicionamiento de contenido de Diversifica, Infografía y Servicios

- **Diversifica:** nueva tesis en meta/OG y cierre ("Diversificar no significa acumular opciones. Significa reducir dependencias sin perder claridad, liquidez ni control."). Se citó explícitamente la fuente del dato del 65% (Tom Corley, "Rich Habits") en las dos secciones donde aparece — antes se mencionaba sin atribución en el cuerpo del texto. La tarjeta de "2ª fuente" dejó de prescribir exclusivamente "Descentralizada" — ahora dice "Nueva" y dentro incluye habilidad monetizada, negocio digital o modelo descentralizado, para no presentar una sola ruta como la correcta para todos. Eliminado el determinismo "cuando algo mejor llega, el mundo avanza y no regresa" (acordeón de tendencia) y la urgencia artificial de "ventana que se cierra" — aparecía dos veces (split tradicional/descentralizado y cierre final: "esa ventana no estará abierta para siempre"). CTA cambiado de "Quiero diversificar mis ingresos" a "Evaluar mi nivel de diversificación", enlazando al diagnóstico funcional de la home (creado en la Fase 2), con "Prefiero platicar directamente" como opción secundaria de WhatsApp.
- **Infografía / El Patrón:** título cambiado de "El patrón que nunca falla" a "El Patrón — cómo evaluar una innovación". Auditoría de lenguaje absolutista: eliminados "nunca falla", "siempre es el mismo", "sin excepción" (títulos/previews de acordeones) y "siempre ha tenido el mismo final" (gancho de apertura, corregido en un commit separado tras notar la inconsistencia contra la nueva tesis ya publicada). Se agregó una aclaración explícita de que el patrón describe innovaciones que sí se adoptaron — muchas tecnologías ridiculizadas simplemente desaparecieron, esto no es garantía de éxito futuro. **Cambio estructural más importante de esta página:** se agregó un acordeón completamente nuevo — "Cómo evaluar una innovación" — con los 11 criterios pedidos (problema real, adopción, infraestructura, incentivos, regulación, seguridad, utilidad, escalabilidad, sostenibilidad, riesgo, evidencia), convirtiendo la página de narrativa histórica pura en una herramienta de evaluación práctica. Cierre reescrito sin "la ventana está abierta"; CTA principal ahora "Aprender a evaluar una innovación" (ancla al nuevo acordeón `#a6`), con "Quiero la conversación" como CTA secundaria de WhatsApp.
- **Servicios → Infinexa Digital:** branding actualizado en title/OG mantniendo la URL `/servicios/` intacta (sin romper SEO ni enlaces existentes desde el resto del sitio). Eliminado por completo el lenguaje de "materiales/textos de prospección" — herencia directa del modelo de negocio de Hand4Hand (venta de kits de reclutamiento) — reemplazado por "materiales comerciales", consistente con el nuevo posicionamiento educativo de Infinexa. Los tres precios ($2,000 / $97 / $797) se mantuvieron sin cambio de monto pero se expresan ahora en USD en vez de exclusivamente USDT, con nota de equivalencia — y se agregó transferencia bancaria tradicional como alternativa de pago igualmente válida (antes el copy y el FAQ decían explícitamente que el pago era "únicamente en USDT"). Aclarado que implementación y mantenimiento se facturan por separado. Metodología del paquete alineada al método Infinexa (analizar → definir → construir → publicar → medir → mejorar).
- **Verificado en vivo tras cada push** (`2520362`, `47285b5`): las tres páginas cargan correctamente con el contenido nuevo, nav intacta, balance de HTML correcto, cero menciones de Hand4Hand o "prospección" en los tres archivos.
- **`ESTADO.md` actualizado** con la sección 1.3 documentando el detalle completo de esta fase.

## 2026-07-13 (continuación) — Fase 4: páginas legales (Transparencia, Riesgos, Privacidad, Términos) y sitemap

- **Creadas 4 páginas nuevas** con el mismo sistema visual del sitio: `/transparencia/`, `/riesgos/`, `/privacidad/`, `/terminos/`. Contenido redactado siguiendo el detalle pedido en el prompt maestro (qué es/no es Infinexa, cómo se verifica y actualiza el contenido, cómo se revelan relaciones comerciales con terceros, 10 categorías de riesgo, qué datos se recopilan realmente — solo diagnóstico/talleres/WhatsApp, sin cuentas ni analítica activa — y condiciones de uso). Todas incluyen nota de que no sustituyen asesoría legal profesional, sin redactarse como si fueran un documento legal definitivo.
- **Enlazadas desde el footer de las 6 superficies del sitio** (antes no existían y no estaban enlazadas desde ningún lado) — cada página tocada necesitó su propio ajuste de footer porque el sitio no tiene un componente de footer compartido (mismo patrón que la nav antes de la Fase 2).
- **`sitemap.xml` actualizado** con las 4 rutas nuevas, prioridad baja y `changefreq: yearly` (contenido legal/informativo, no editorial).
- **Verificado en vivo tras el push** (`07a8512`): las 4 páginas cargan correctamente con nav y footer consistentes, balance de HTML correcto, sitemap.xml bien formado.
- **`ESTADO.md` actualizado** con la sección 1.4.

## 2026-07-13 (continuación) — Fase 5: auditoría de claims del blog

- **Sweep con grep** sobre los 15 posts de `_posts/` buscando lenguaje absolutista, garantías de rendimiento, urgencia artificial y superlativos sin fuente. La gran mayoría de coincidencias ("siempre"/"nunca") resultaron ser usos narrativos normales, no claims financieros problemáticos.
- **Corregido `stablecoin-vs-bitcoin.md`:** describía la paridad de USDT como absoluta ("siempre exactamente un dólar", "siempre hay con qué canjear"). Reescrito como objetivo de diseño, no garantía, agregando una frase explícita sobre riesgo de de-peg y el escrutinio público de las reservas de Tether.
- **Corregidas referencias obsoletas a "la carta"** en 4 posts (`wallet-no-custodial`, `stablecoin-vs-bitcoin`, `bloque-genesis-bitcoin`, `whitepaper-bitcoin`) y en `donativo/index.html`: apuntaban a `/` describiéndolo como "la carta de Infinexa", narrativa que ya no existe ahí desde que la Fase 2 reconstruyó la home por completo. Cambiado a "Infinexa" de forma genérica, tanto en `pillar_label` del front matter como en los enlaces del cuerpo.
- **Verificado:** sin lenguaje de rendimiento garantizado ni urgencia artificial en ninguna página publicada. La estadística del 65% no aparece sin fuente en el blog (remite a Diversifica, ya citada desde la Fase 3). Un par de coincidencias de "garantiza"/"aseguran" en `historia-del-dinero.md` y `piezas-rompecabezas.md` se revisaron en contexto y no requerían cambio.
- **`sitemap.xml` actualizado** con `lastmod: 2026-07-13` en los 4 posts editados.
- **Commit `80528bd`, push a producción.** `ESTADO.md` actualizado con la sección 1.5.
- Pendiente: decisión sobre `builders/carlos`, analítica real (GA4/GTM), accesibilidad, informe final consolidado de cumplimiento (sección 31 del prompt maestro).

## 2026-07-13 (continuación) — decisión sobre builders/carlos y auditoría de accesibilidad

- **Alejandro confirmó, vía pregunta directa:** dejar `builders/carlos`, `builders/_template` y `carlos/` sin tocar por ahora (ni aviso a Carlos, ni baja del subdominio, ni migración). Sigue siendo una decisión abierta a futuro. También confirmó mantener Diversifica e Infografía como páginas standalone enlazadas desde el home, sin fusionarlas al nav superior ni al blog.
- **Auditoría de accesibilidad ejecutada (commit `ececf51`):** revisión de teclado/foco/contraste/aria/labels en las 6 superficies + 4 páginas legales. La mayoría ya estaba bien (labels de formularios asociados, aria-expanded en acordeones, lang="es" en todas). Hallazgo real medido con fórmula de contraste WCAG: el disclaimer de cumplimiento se renderizaba a ~2.1:1 (muy por debajo del mínimo 4.5:1 AA) en 8 páginas — corregido subiendo opacidad de .3 a .6 (resultado ~4.9-5.0:1). `donativo/index.html` tenía el mismo problema a menor escala (4.2:1 → 6.2:1). Se descubrió que `servicios/index.html` era la única página pública sin ninguna frase de disclaimer — se agregó una, y se subió el contraste de sus enlaces de footer.
- Verificado balance de HTML en las 10 páginas tocadas antes de publicar.
- `ESTADO.md` actualizado con las secciones 1.6 y 1.7.
- Pendiente: analítica real (GA4/GTM — falta ID de Alejandro), informe final consolidado (sección 31 del prompt maestro).

## 2026-07-13 (continuación) — informe final y Google Analytics 4 conectado

- **Informe final consolidado entregado** (`Infinexa_Informe_Final_13jul2026.docx`, fuera del repo): resume las 6 fases ejecutadas, verificaciones, decisiones tomadas por Alejandro, pendientes abiertos, riesgos residuales y una recomendación GO.
- **Alejandro proporcionó el ID de GA4** (`G-X8LW9B8JP2`). Conectado en el `<head>` de las 9 páginas standalone + `_layouts/default.html` (commit `4d43ff8`) — el helper `trackEvent()` deja de ser un no-op.
- **`/privacidad/` actualizada** para cumplir su propia promesa de reflejar cualquier herramienta de analítica antes de activarla: ahora describe el uso de GA4, cookies, separación de datos de WhatsApp/diagnóstico/talleres, opción de bloqueo, y enlace a la política de Google.
- Verificado balance de HTML en las 10 páginas tocadas antes de publicar.
- `ESTADO.md` actualizado con las secciones 1.8 y 1.9.
- Único pendiente real que queda abierto: decisión futura sobre `builders/carlos` (ver entrada anterior) y una auditoría de SEO técnico más amplia (títulos, canonicals, structured data, Core Web Vitals).

## 2026-07-13 (continuación) — builders/carlos eliminado por completo

- **Alejandro revirtió su propia decisión anterior** (dejarlo sin tocar) y pidió eliminar la página por completo: "prácticamente quedó vacía... lo mejor será eliminarla de una vez".
- Antes de ejecutar, se investigó el contenido real: `builders/carlos/index.html` (144 líneas, landing completa y funcional, NO vacía) vs. `builders/carlos/infografia/index.html` (0 bytes, literalmente vacío, nunca se terminó — igual que `builders/demo/infografia/`, también vacía). Se confirmó con Alejandro, de forma explícita, que el alcance era eliminar **todo** `builders/carlos/`, no solo el archivo vacío, entendiendo que esto tumba `carlos.infinexa.app` sin aviso previo a Carlos.
- **Ejecutado** (`git rm -r builders/carlos`, commit `5ca2dc7`): eliminados `builders/carlos/index.html` y `builders/carlos/infografia/index.html`.
- **Hallazgo adicional:** existía un `carlos/index.html` huérfano en la raíz del repo — un template roto con placeholders sin rellenar (`{{NOMBRE}}`, `{{APODO}}`), públicamente accesible en `infinexa.app/carlos/`. No era la página en vivo del subdominio; era un duplicado abandonado de una versión anterior del sistema de builders. Se eliminó también, como limpieza relacionada (no pedida explícitamente, pero comunicada).
- `builders/_template/` y `builders/demo/` no se tocaron — siguen como scaffold para futuros builders.
- Verificado en vivo tras el push: `infinexa.app/carlos/` y `carlos.infinexa.app` devuelven vacío/404.
- `ESTADO.md` actualizado con la sección 1.10 (y nota de reversión en 1.6).

## Cómo seguir esta bitácora

- Cada sesión de trabajo nueva, pide a Claude: "agrega una entrada a la bitácora de hoy" al final de la conversación.
- Si una sesión se extiende por varios días, usa la fecha de inicio como encabezado y ve agregando viñetas conforme avanza.
- No reescribas entradas viejas — si algo cambió después, se documenta como una entrada nueva que referencia a la anterior (ej. "corregido el bug de X que se reportó el [fecha]").

## 2026-06-27 (sesión larga — unicidad, legibilidad de imágenes, ritmo visual, sistema de imágenes destacadas)

- **Auditoría de unicidad de los 4 posts publicados:** se detectó que los 4 repetían la misma plantilla en tres puntos (encabezado "La pregunta que Carlos...", gancho inicial "¿Alguna vez te has preguntado...?", y cierre siempre en prosa con 3 opciones y el callback a Carlos en la última línea) — exactamente lo que la sección 5 de `BLOG_GUIA.md` prohíbe. Reescritos los posts 2, 3 y 4 variando gancho, encabezado y estructura de cierre (lista vs. prosa, 2 vs. 3 opciones, posición del callback), dejando el post 1 como referencia original. Cero cambios a título, slug o front matter — cero riesgo SEO.
- **Auditoría de legibilidad de imágenes de apoyo:** las 8 imágenes con texto en columnas (3-4 por imagen) se volvían ilegibles en celular — el texto nace a 1200px y se escala junto con la imagen hasta ~345px reales en pantalla, cayendo a 4-5px de altura. Confirmado matemáticamente, no a ojo.
- **Decisión de arquitectura:** las 8 imágenes de tarjetas/comparación se convirtieron a HTML/CSS nativo (clases nuevas en `_layouts/post.html`: `.cards-row`, `.card-box`, `.timeline-row`, `.section-label-inline`, documentadas en la sección 9.1 de la guía). Verificado con búsqueda web que esto además ayuda SEO (texto horneado en imagen es invisible para Google) y Core Web Vitals (menos peso de página). Las imágenes destacadas y el estilo "frase grande" se quedan como imagen — ahí sí funcionan.
- **Ritmo visual tipográfico:** se detectó que el `blockquote` ya tenía estilo en CSS pero nunca se usaba (las citas de Carlos se escribían en cursiva inline, no como bloque real). Agregadas tres herramientas nuevas — `.lead` (párrafo de apertura), blockquote real (`>`) para diálogo citado, y `.insight` (la frase "ajá" central, con líneas divisorias) — documentadas en la nueva sección 4.1. Aplicadas a los 4 posts.
- **Sistema de imágenes destacadas rediseñado:** se evaluaron 9 imágenes de referencia que el usuario encontró (estilo fotorrealista, constelaciones, gente en amaneceres) — se rescató el concepto de constelación (coherente con el logo del infinito) pero se descartó el fotorrealismo, las figuras humanas y las poses de "triunfo financiero" (riesgo de cumplimiento: visualmente prometen el mismo resultado financiero que el copy tiene prohibido decir en texto). Construido `_plantillas/sistema-imagenes-blog/generar_destacadas.py` (Playwright + SVG) con sistema de íconos fijos por pilar (candado / bifurcación / ciclo).
- **Error y corrección — logo redibujado a mano:** al construir el ícono de marca para las imágenes destacadas, se redibujó el path del logo a mano en vez de copiar las coordenadas exactas del original (`codigo-final-render-og-image.py`) — resultó en un infinito con proporciones incorrectas y sin el detalle cobre del cruce. Corregido reutilizando el `<path>` original sin modificar, envuelto en `transform="translate() scale()"`. **Lección permanente: ver sección 9.2 de `BLOG_GUIA.md` — el logo y cualquier elemento de marca SIEMPRE se copian del activo original, nunca se redibujan a mano.**
- **Error y corrección — balance compositivo:** la primera versión de las imágenes destacadas concentraba todo el contenido (texto + ícono) en el lado izquierdo, dejando el lado derecho casi vacío — se veía desbalanceado a cualquier tamaño. Corregido agregando un cluster de nodos de constelación más grande del lado derecho como contrapeso visual. **Lección permanente: ver checklist de la sección 9.2 — siempre revisar balance compositivo y simular a ancho de celular antes de entregar.**
- **Error y corrección — `.post-body` sin centrar:** el contenedor de texto del post tenía `max-width:780px` pero le faltaba `margin:0 auto`, por lo que en pantallas anchas el texto quedaba pegado al borde izquierdo de la tarjeta de 1100px (mientras la imagen de cabecera sí ocupaba el ancho completo) — esto, no la imagen, era la causa real de "se ve cargado a un lado". Corregido agregando `margin:0 auto`. **Lección permanente: todo bloque con `max-width` fijo debe llevar `margin:0 auto` junto con él.**
- **Aclarado un falso positivo:** tras el fix del logo, el usuario seguía viendo la versión vieja — se confirmó comparando el archivo real publicado en GitHub (idéntico byte a byte al generado) que era caché del navegador/CDN, no un error real. **Lección permanente: ante "la corrección no se ve", comparar primero el archivo publicado en GitHub contra el generado, antes de asumir que hay que regenerar algo.**
- Agregada sección 9.2 a `BLOG_GUIA.md` consolidando las 4 lecciones de esta sesión como reglas fijas (activo original nunca redibujado, checklist de 4 puntos antes de entregar, `margin:auto` con `max-width`, y caché al sobrescribir imágenes).
- 8 imágenes huérfanas eliminadas del repo tras la conversión a HTML.
- Commits de la sesión: `6343981`, `ff09d25`, `43f64ee`, `4c99447`, `91e7dfa`, `8a5696d`.

## 2026-06-29 (subtítulo del blog, principio de promoción vs. prevención, propuestas de post #5, decisión sobre el menú de navegación)

- **Subtítulo del blog corregido dos veces:** de "sin presión, con datos verificados" (prevención decorativa, sin objeción real detrás) a "no le teme al cambio" (seguía partiendo de negación) a la versión final: "porque quien entiende el patrón, ve la puerta antes que los demás" (promoción — parte de una posibilidad, no de un miedo).
- **Principio de promoción vs. prevención documentado** en la nueva sección 4.2 de `BLOG_GUIA.md`, con la línea exacta de dónde está el límite con "promesa falsa" (se promete claridad y perspectiva, nunca resultado financiero). Incluye la excepción ya auditada: "sin presión" aparece 8 veces en el resto del sitio, siempre junto a una invitación a llamada — ahí es legítimo porque responde a una objeción real y nombrada, y esos 8 casos no se tocan.
- **`ESTADO.md` actualizado:** el blog no aparecía en ningún lado del estado general del proyecto — agregada fila a la tabla de páginas publicadas, nueva subsección 4.4 con resumen y referencia cruzada a `BLOG_GUIA.md`/`BITACORA.md`, y el post #5 agregado a "Próximos pasos inmediatos".
- **4 propuestas de post #5 investigadas con búsqueda real** (no solo intuición): stablecoins (pilar la carta), historia del cajero automático como espejo de la desconfianza actual hacia las wallets (pilar El Patrón), historia del dinero del trueque a lo digital (ancla para el cuarto pilar de storytelling histórico, que hasta ahora no tenía ningún post), e ingreso pasivo (pilar Diversifica, esta última sin validar con búsqueda todavía). Pendiente decidir cuál escribir primero.
- **Propuesta descartada — ocultar el catálogo de posts en el índice del blog:** el usuario preguntó si convenía limitar el acceso a las entradas anteriores para generar "sorpresa". Se evaluó y se desaconsejó: dañaría el rastreo/indexación de Google, quitaría la forma más fácil de descubrir el resto del contenido, y contradice la filosofía de faro (visible siempre, no racionado).
- **Propuesta evaluada con datos — reducir el menú de navegación solo en páginas del blog** (dejar solo "Blog", quitando Inicio/Diversifica/El Patrón): investigación real mostró que quitar el menú sube conversiones de forma dramática (16-100%+) solo en páginas de un solo objetivo de conversión directa (estudios de HubSpot/CXL/Yuppiechef) — en páginas de "tope de embudo" como un blog educativo, la mejora medida es de apenas 0-4%. El costo (enlazado interno perdido hacia Diversifica/El Patrón, más significativo en un sitio chico de 5-6 páginas) pesa más que ese beneficio casi nulo. **Decisión: mantener el menú completo en el blog.** Criterio de decisión completo guardado en la nueva sección 1.1 de `RECETAS.md`, para cualquier página futura que sí sea de conversión directa de un solo paso.

## 2026-06-30 (post #5 publicado, audio del primer post vía ElevenLabs, página de donativo, reincidencia del bug de centrado)

- **Post #5 publicado:** "El cajero automático y la misma desconfianza de hoy" (pilar El Patrón), 1,325 palabras, con `.lead`/blockquote/`.insight`, chequeo de unicidad limpio contra los 4 posts existentes. Imagen destacada generada con el sistema ya establecido. Se descubrió que `sitemap.xml` solo tenía el post #1 desde hacía semanas — se corrigió agregando los 4 posts faltantes.
- **Audio del primer post (Ingreso vs. activo) vía ElevenLabs:** se evaluó con datos la opción de "leer en voz alta" — se descartó la opción de audio pregenerado con IA como estándar para todos los posts (cada edición futura del texto obligaría a regenerar el audio completo, retrabajo permanente) pero se decidió hacerlo puntualmente para este post a petición explícita. Reproductor HTML embebido con CSS de marca.
- **Incidente — texto truncado en el audio:** la primera generación en ElevenLabs perdió contenido a mitad de texto. Diagnóstico: el límite de 5,000 caracteres por generación se acercó demasiado sin margen de seguridad. **Protocolo de verificación documentado en la nueva sección 9.3 de `BLOG_GUIA.md`**: margen de seguridad de 4,500 caracteres, verificación del contador en pantalla antes de generar, puntos de control al unir clips en QuickTime, y marcación explícita de la frase de cierre/apertura entre partes. Aplica a cualquier página futura con audio, no solo al blog.
- **Página `/donativo/` creada:** apoyo voluntario en USDT (red Polygon) y Bitcoin, con QR generados y verificados por decodificación (confirmado que ambos QR devuelven exactamente las direcciones correctas, sin error de transcripción), botón de copiar dirección, advertencia de red, y lenguaje de cumplimiento explícito ("esto es un donativo, no un pago ni una inversión"). Texto agregado indicando escanear desde la app de wallet y no desde la cámara del teléfono, para evitar confusión. Página publicada pero deliberadamente sin enlazar todavía desde ningún lado del sitio.
- **Reincidencia del bug de centrado (`max-width` sin `margin:auto`):** el mismo error que ya se había corregido y documentado el 27 jun (sección 9.2, punto 3) volvió a aparecer en la página `/donativo/`, porque es una página nueva escrita desde cero y la regla no se aplicó activamente al escribir el CSS, solo se recordó después de que el usuario lo reportó en producción. **Lección reforzada:** la sección 9.2 ahora exige correr `grep -n "max-width"` sobre cualquier HTML/CSS nuevo antes de entregarlo, y confirmar manualmente que cada resultado tenga `margin:0 auto` — ya no es una nota pasiva, es un paso de verificación obligatorio.
- Diferenciado, a petición del usuario, entre "construir el motor de IA de texto a voz" (inviable, requiere equipos de ML e infraestructura a escala de cientos de millones de dólares) y "construir un producto usando una API existente como ElevenLabs" (viable, mismo patrón que cualquier otro desarrollo de software) — tema pausado para retomar después.

## 2026-07-06 (sesión de marketing — arte + texto WhatsApp para posts del blog)

- **Framework PAS + Gancho Deslizante validado:** se investigaron y compararon frameworks de copywriting para WhatsApp corto (PAS, AIDA, Hook-Story-Offer, Slippery Slope de Sugarman). Se seleccionó PAS + Gancho Deslizante como el más adecuado para mensajes educativos cortos. Investigación con fuentes reales: Buffer, Copy Brothers, Smartbrand, QuickReply.ai. Estructura fija: Realidad → Agitación → Puente → CTA doble → Difusión. Máximo 5 líneas + firma. Documentado en RECETAS.md sección 11.
- **Arte PNG generado para post #1 (Ingreso vs. activo):** primera pieza de la serie de artes por post. 680×920px, ~62KB, pipeline Playwright + Chromium. Dos versiones iteradas: v1 (texto 12px ilegible en móvil, columnas poco contrastadas) → v2 corregida (14px mínimo, fondos sólidos opacidad 0.7+, gancho más visible, frase central en blanco). Hilo conductor validado: post ↔ arte ↔ texto alineados en mismo dolor, concepto y destino.
- **Error de URL corregido y documentado como regla permanente:** URL `/blog/ingreso-vs-activo-la-diferencia/` usada en copy de WhatsApp resultó en 404 — slug inventado sin verificar. Diagnóstico con `head -5 _posts/2026-06-25-ingreso-vs-activo.md` reveló slug correcto: `/blog/ingreso-vs-activo/`. Causa raíz: Jekyll forma el slug desde el nombre del archivo, no desde el título. Regla documentada en RECETAS.md sección 11.3 y PROMPT_MAESTRO.md Regla de Oro #5.
- **Lección de tono México documentada:** término "claveteo" desconectó al usuario antes de terminar de leer — una sola palabra desconocida rompe el hilo completo. Regla: frases de uso común y amplio, sin regionalismos poco conocidos. Documentado en RECETAS.md sección 11 y PROMPT_MARKETING.md.
- **Sistema de PROMPT_*.md descubierto en sesión:** al revisar el Finder se encontraron 5 archivos nuevos en `_gestion/` (PROMPT_MAESTRO, PROMPT_MARKETING, PROMPT_BLOG, PROMPT_DISENO, PROMPT_PAGINAS) creados el 1 jul 2026. Esto ajustó el plan de actualización: se evitó duplicar en RECETAS.md lo que ya estaba en PROMPT_MARKETING.md.
- **Archivos actualizados:** RECETAS.md (sección 11), PROMPT_MARKETING.md (subsección Promoción de posts), PROMPT_MAESTRO.md (tabla de protocolos + Regla de Oro #5), ESTADO.md (sección 7), BITACORA.md (esta entrada).

## 2026-07-12 (posts #11, #12 y #13 publicados — arco Bitcoin; corrección de bug de orden en `.principio`; corrección de duplicidad título/imagen en 4 posts)

- **Post #11 corregido — orden de bloques `.principio`:** el post publicado en la sesión anterior (Claude Code) tenía los badges en orden 1→3→2 en vez de 1→2→3, con los dos últimos bloques pegados uno justo después del otro sin párrafo de transición. Detectado por el usuario vía captura de pantalla del post en vivo. Corregido: badges renumerados en orden secuencial, y se agregó una frase puente ("Sumadas, esas cuatro capas explican...") entre ambos bloques para que no queden apilados. Commit `ec6cd8e`.
- **Posts #12 y #13 escritos y publicados** desde esta conversación (no Claude Code), replicando el proceso completo: clonado del repo público (`Appsalex/infinexa`) para leer `BLOG_GUIA.md` completo y verificar unicidad contra los posts reales, generación de imagen con el script real (Playwright funcional en el entorno de esta conversación), checklist de 4 puntos, conversión a WebP, actualización de sitemap. Commits `76ce15a` (#12) y `063aa13` (#13).
- **Bug nuevo descubierto — título del post idéntico al texto de la imagen:** desde el post #10 (inclusive) hasta el #13, el `title` del front matter se volvía copia literal del texto corto de la imagen, partido en dos líneas — rompiendo el patrón ya resuelto en los posts #1–#9, donde la imagen lleva una versión corta y el post una versión expandida con contexto adicional (ej. "Ingreso vs. activo" en la imagen vs. "Ingreso vs. activo: la diferencia que cambia cómo ves tu dinero" en el post). Detectado por el usuario comparando una captura antigua (resuelta) contra el patrón actual (roto). **Corregido en los 4 posts del arco Bitcoin** (#10, #11, #12, #13): título de imagen acortado y reescrito de forma distinta, título del post expandido con estructura "frase corta: contexto adicional". Imágenes regeneradas con el script real. Commit `393e5d0`.
- **Dos reglas nuevas documentadas en `PROMPT_BLOG.md` (regla 6 y regla 7)** para que ninguno de los dos bugs se repita: verificación explícita de que título de imagen ≠ título de post, y verificación de que los badges de `.principio` estén en orden secuencial antes de cada commit. Ambas agregadas también como paso de verificación obligatorio en el "Paso 6" del proceso paso a paso.
- **Estado del arco Bitcoin al cierre de esta sesión:** 4 de 6 posts publicados (#10, #11, #12, #13). Pendientes: #14 (nodos y mineros, PoW vs. PoS) y #15 (cierre del arco, tono educativo con CTA suave — decisión ya tomada con el usuario).
- **Archivos actualizados:** PROMPT_BLOG.md (versión 1.1 — estado del blog, tabla de posts, reglas 6 y 7, paso 6 del proceso), BITACORA.md (esta entrada).

## 2026-07-12 (continuación) — Posts #14 y #15 publicados (arco Bitcoin completo); incidente de infraestructura: caída total del sitio y resolución

- **Posts #14 y #15 escritos y publicados**, cerrando el arco Bitcoin completo (6/6). #14 "Quién guarda la verdad cuando nadie está a cargo" (nodos/mineros, PoW vs. PoS, pilar El Patrón) y #15 "De un experimento de nueve páginas a la infraestructura del dinero de hoy" (cierre del arco, pilar Diversifica, tono educativo con CTA suave hacia `/diversifica`, sin CTA fuerte hacia Hand4Hand). Commits `9689e82` y `1e68b85`.

- **Bug de orden del blog descubierto:** el usuario notó que el post #15 (el cierre, más reciente) aparecía en la segunda fila del índice del blog en vez de en la posición esperada. Causa raíz: ningún post tenía campo `date:` explícito en el front matter — Jekyll tomaba la fecha solo del nombre de archivo (sin hora), y **4 posts compartían exactamente la misma fecha** (`2026-07-12`: whitepaper, bloque génesis, nodos, cierre) y otros 6 posts más antiguos también compartían fecha entre sí (3 el `2026-06-26`, 3 el `2026-07-01`). Con fechas idénticas, Jekyll resuelve el empate de forma no determinística (orden alfabético del archivo), no por el orden real de publicación.

- **Corrección aplicada:** se agregó campo `date: YYYY-MM-DD HH:MM:SS -0700` explícito a los 12 posts afectados, con horas espaciadas para reflejar el orden real de publicación. Commit `16aaa9b`. Verificado con una simulación del ordenamiento: el resultado coincide exactamente con la numeración real de los posts (#15 arriba, #1 al final).

- **Bug introducido por la corrección anterior — `future: false` de Jekyll:** al agregar horas específicas con zona horaria `-0700`, tres de los cuatro posts del 12 de julio quedaron con hora UTC posterior al momento del build de GitHub Pages (ej. 12:00 -0700 = 19:00 UTC). Jekyll, por defecto, **oculta silenciosamente** (sin error) cualquier post cuya fecha/hora sea posterior al momento del build. Esto hizo que los posts #13, #14 y #15 desaparecieran por completo del blog en vivo, aunque seguían existiendo en el repo y las Actions de GitHub mostraban builds exitosos. **Corregido agregando `future: true` a `_config.yml`** (commit `59ce8ea`) — esto evita depender de la hora exacta del build para siempre, sin importar qué fechas se usen en el futuro.

- **Incidente mayor — caída completa del sitio (infinexa.app inaccesible, ERR_TIMED_OUT):** después del fix de `future: true`, el usuario reportó que el sitio completo (no solo el blog) cargaba extremadamente lento y luego, en visitas posteriores, dejaba de cargar por completo (`ERR_TIMED_OUT`), tanto en `infinexa.app` como en la URL cruda de GitHub Pages (`appsalex.github.io/infinexa/blog/`). Diagnóstico paso a paso:
  1. Se descartó cualquier problema de código/YAML (los 15 posts y `_config.yml` se validaron sintácticamente correctos).
  2. Se confirmó que los builds de GitHub Actions estaban 100% exitosos (113 corridas, todas en verde, ~40 segundos cada una).
  3. Se encontró que GitHub Pages mostraba `http://infinexa.app/` (sin HTTPS) como URL activa — el certificado SSL del dominio personalizado no se había emitido, porque el registro DNS raíz (`infinexa.app`) estaba en modo **Proxied** (nube naranja) en Cloudflare, lo cual bloquea la validación ACME de GitHub. Se corrigió temporalmente pasando el registro a DNS only, esperando la emisión del certificado, y confirmando `https://infinexa.app/` activo con "Enforce HTTPS" disponible.
  4. Aun con el certificado emitido, el sitio seguía sin cargar. Se encontró la causa real: el **Worker `infinexa-builders` tenía una ruta extra `infinexa.app/*`** (además de la correcta `*.infinexa.app/*`), interceptando todo el tráfico del dominio raíz y evitando que llegara a GitHub Pages. Se eliminó esa ruta, dejando solo `*.infinexa.app/*`.
  5. El sitio seguía sin cargar exclusivamente en los dispositivos personales del usuario (MacBook y iPhone), mientras que un tercero en otra red sí podía acceder sin problema — aislando el problema a **iCloud Private Relay**, que tenía cacheada la resolución rota de antes de las correcciones (afecta por igual a dispositivos que comparten cuenta de Apple, sin importar la red). Se resolvió desactivando Private Relay temporalmente en ambos dispositivos.
  6. **Configuración final e intencional, documentada en la sección 2 de ESTADO.md:** dominio raíz (`infinexa.app`, `www.infinexa.app`) permanentemente en **DNS only** — inmune a cualquier futuro error de ruteo en Workers. Wildcard (`*.infinexa.app`) en **Proxied**, único lugar donde el Worker `infinexa-builders` puede interceptar tráfico. Se verificó que el blog principal y un subdominio de builder de prueba funcionan correctamente tras la corrección.

- **Lección para futuras sesiones:** nunca activar el proxy de Cloudflare en el dominio raíz de `infinexa.app`, y siempre confirmar que el Worker `infinexa-builders` tenga únicamente la ruta `*.infinexa.app/*` antes de tocar cualquier configuración de DNS o Workers Routes. Ver advertencia añadida en ESTADO.md sección 2.

- **Archivos actualizados:** `_config.yml` (`future: true`), 12 archivos en `_posts/` (campo `date:`), `ESTADO.md` (sección 2 con advertencia de infraestructura, sección 4.4 con los 15 posts, arco Bitcoin marcado completo), `BITACORA.md` (esta entrada).
